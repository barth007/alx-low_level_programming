#include "main.h"

ssize_t read_textfile(const char *filename, size_t letters)
{
	size_t n;
	int
	void *buff;

	buff = malloc(letters * sizeof(size_t));

	read(STDIN_FILENO,buff,letters);
	n = write(STDOUT_FILENO,filename,letters);
	return (n);
}
